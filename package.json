{
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^20.9.0"
  },
  "name": "website2pdf",
  "description": "This project aims to provide a user-friendly platform for extracting PDFs from websites. Users can enter website URLs, and the server will convert them into PDFs, which can then be saved on the server. Users have the option to download the PDFs separately or merged.",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=16.3.0"
  },
  "author": "Jaka Pelko",
  "license": "ISC"
}
